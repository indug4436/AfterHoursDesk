/**
 * Paper-agent stub — Desk demo only. No orders. Optional Agentic widen later.
 */
export function paperPlan(_body) {
  return {
    status: 'stub',
    message: 'paper-agent not enabled — research Desk only',
  };
}
